@model List<ApplicationUser>

<h3 align="center">Student User Accounts List</h3>
@if (TempData["SuccessA"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        <strong>@TempData["SuccessA"]</strong>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>

}
@if (TempData["SuccessB"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        <strong>@TempData["SuccessB"]</strong>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>

}
@if (TempData["SuccessC"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        <strong>@TempData["SuccessC"]</strong>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>

}

<a class="btn btn-success" asp-action="Register" asp-controller="Account"><i class="bi bi-plus-circle"></i> Register New Account</a>
<br />
<table class="table table-striped table-info" id="data">
    <thead class="thead-dark">
        <tr>
            <th scope="col">#</th>
            <th scope="col">Name</th>
            <th scope="col">Email Address</th>
            <th scope="col"></th>
            <th scope="col"></th>
            <th scope="col"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var std in Model)
        {
            <tr>
                <th scope="row"></th>
                <td>@std.FirstName @std.LastName</td>
                <td>@std.Email</td>
                <td><a class="btn btn-primary" asp-controller="Account" asp-action="ViewOneStudent" asp-route-id="@std.Id"><i class="bi bi-eye"></i> View</a></td>
                <td><a class="btn btn-warning" asp-controller="Account" asp-action="UpdateStudent" asp-route-id="@std.Id"><i class="bi bi-pencil-square"></i> Edit</a></td>
                <td><a class="btn btn-danger" asp-controller="Account" asp-action="DeleteStudent" asp-route-id="@std.Id"><i class="bi bi-trash"></i> Delete</a></td>
            </tr>
        }
    </tbody>
</table>
